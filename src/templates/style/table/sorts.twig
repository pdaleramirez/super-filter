<h1>This is table type</h1>
<ul>
    {% for item in items %}
        <li>
            {{  item.title }} - {{ item.dateCreated.format('Y-m-d H:i:s') }} - {{ item.superFilterImdbRating }}
        </li>
    {% endfor %}
</ul>
